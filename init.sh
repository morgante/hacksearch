#!/bin/bash

# todo: do which statements so things aren't reinstalled

npm install -g foreman
npm install -g supervisor
npm install -g grunt-cli
npm install -g bower
bower install
gem install compass
gem install zurb-foundation
